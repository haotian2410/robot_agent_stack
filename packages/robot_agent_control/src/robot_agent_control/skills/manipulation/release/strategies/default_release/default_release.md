# Default Release

Open a compatible gripper at the current pose, detach the held payload, and return state evidence for unified verification.

This strategy never moves the robot arm and never evaluates placement-region correctness.
